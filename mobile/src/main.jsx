import React from 'react';
import ReactDOM from 'react-dom/client';
import DoughTempTracker from '../dough-temp-tracker';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <DoughTempTracker />
    </React.StrictMode>
);
